# ═══ LLM ═══
OPENAI_API_KEY=sk-...
DIRECTOR_MODEL=gpt-4o
DIRECTOR_TEMPERATURE=0.1

# ═══ CONTROLES DO LOOP ═══
DIRECTOR_MAX_ITERATIONS=10
DIRECTOR_MAX_SANDBOX_CALLS=3
DIRECTOR_MAX_RERENDERS=2
DIRECTOR_BUDGET_LIMIT_USD=0.50
DIRECTOR_SANDBOX_TIMEOUT=30

# ═══ v-api (interno via docker network) ═══
V_API_INTERNAL_URL=http://v-api:5000
V_API_SERVICE_TOKEN=eyJ...  # SERVICE_ROLE_KEY do Supabase

# ═══ v-llm-directors (Level 0) ═══
V_LLM_DIRECTORS_URL=http://v-llm-directors:5025

# ═══ Modal Sandbox (futuro — Fase 4) ═══
MODAL_SANDBOX_URL=https://fotovinicius2--v-sandbox-execute-code.modal.run

# ═══ Database (audit logging) ═══
DATABASE_URL=postgres://user:pass@host:5432/db?sslmode=prefer

# ═══ Redis ═══
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# ═══ Segurança ═══
DIRECTOR_ALLOW_INTERNET_SANDBOX=false
DIRECTOR_ALLOWED_TOOLS=all
DIRECTOR_LOG_LEVEL=full
